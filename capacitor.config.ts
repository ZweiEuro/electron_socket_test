import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'electron_socket_test',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
